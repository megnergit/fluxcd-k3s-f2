apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: dev
  namespace: flux-system
spec:
  path: ./clusters/dev
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  resources:
    - ./apps/namespace.yaml  # ここを追加！
    - ./apps/nginx/

